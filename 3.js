// Function to handle adding items to the cart
function addToCart(productName, price) {
    // For now, let's simulate adding the product to the cart by showing an alert
    alert(`"${productName}" added to cart. Price: $${price}`);
    // You can further process this by storing in cart, using APIs, etc.
}